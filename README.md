# WordPress .htaccess  starter

This is a starter .htaccess for a WordPress installation that uses an Apache web server.
It forces https connections, set expire headers and uses gzip compression to improve performance.
